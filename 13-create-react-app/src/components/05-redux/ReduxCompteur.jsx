import React from 'react';

function ReduxCompteur(props) {
    return (
        <React.Fragment>
            <h1>Compteur: {null}</h1>
            <button onClick={null}>
                Incrementer
            </button>
        </React.Fragment>
    );
}

export default ReduxCompteur;